{
  "name": "@loom/ws-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/main.ts",
    "start": "tsx --env-file=../../.env src/main.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "TEST_DATABASE_URL=${TEST_DATABASE_URL:-postgres://loom:loom@localhost:5432/loom_test_gateway} vitest run"
  },
  "dependencies": {
    "@fastify/websocket": "^11.3.0",
    "@loom/api-contract": "workspace:*",
    "@loom/domain": "workspace:*",
    "fastify": "^5.12.3",
    "ioredis": "^5.11.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@loom/db": "workspace:*",
    "@loom/server": "workspace:*",
    "@orpc/client": "^1.14.10",
    "@orpc/contract": "^1.14.10",
    "@types/ws": "^8.18.1",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11",
    "ws": "^8.21.3"
  }
}
