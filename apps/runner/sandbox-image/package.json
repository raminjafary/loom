{
  "name": "loom-agent-sandbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "Deliberately NOT part of the pnpm workspace. See Dockerfile.sandbox for why: the Agent SDK ships a per-platform native CLI as an optional dependency, and pnpm's symlinked layout left it dangling inside the image, so the sandbox installs flat with npm instead. Keeping this manifest separate also means the sandbox image contains only what an agent needs, not every workspace devDependency.",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.220",
    "@loom/domain": "file:./packages/domain",
    "@loom/runner-protocol": "file:./packages/runner-protocol",
    "tsx": "^4.20.6",
    "zod": "^4.4.3"
  }
}
